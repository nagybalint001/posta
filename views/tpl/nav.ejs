<% stylesheet('/styles/local/main-style.css') %>
<!-- Nav start -->
<nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark d-print-none">
    <div class="container">
        <div class="navbar-header">
            <a class="navbar-brand" href="/">Post <strong>IT</strong></a>
        </div>
        <button class="navbar-toggler collapsed" type="button" data-toggle="collapse" data-target="#main-nav" aria-controls="main.nav" aria-expanded="false"
            aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
        <div class="collapse navbar-collapse navbar-right" id="main-nav">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a class="nav-link" href="/packages"><i class="fa fa-envelope"></i> <%= __('Packages') %></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/profil"><i class="fa fa-user"></i> <%= __('Profil (%s)', req.session.user.fullname) %></a>
                </li>
                <% if(req.session.user.is_admin == true) { %>
                <li class="nav-item">
                    <a class="nav-link" href="/admin"><i class="fa fa-lock"></i> <%= __('Admin') %></a>
                </li>   
                <% } %>
            </ul>
            <ul class="navbar-nav">
                <li>
                    <a class="nav-link" href="/logout"><i class="fa fa-sign-out"></i> <%= __('Logout') %></a>
                </li>
            </ul>
        </div>
    </div>
</nav>
<!-- Nav end -->